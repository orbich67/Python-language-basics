Задача 1.   Сделал. Исходный файл nginx_logs.txt в архиве с практическим заданием не отправлял.

Задача 2.*  Не делал.

Задача 3.   Решение сделал в виде функции (аргументы - имена файлов). Для выполнения условий задачи с разной длиной
            списков users, hobby использовал индексы списков (не получилось применить zip и генераторы).
            Сформированный словарь записал в файл используя модуль json (так в файле видно, что сохранен именно
            словарь). Проверил сохранненные данные обратной загрузкой данных из файла.

Задача 4.*  Сделал.

Задача 5.** Не делал.

Задача 6.   Сделал. Заполненный файл bakery.csv не отправлял, т.к. он создается во время работы скрипта add_sale.py.
            Продажи можно вносить списком через пробел. Наверное плохая идея печатать из функции, но я больше ничего
            не придумал (в скрипте show_sales.py функция show_sales вызывается из функции main через print).

Задача 7.*  Не делал.